#ifndef _InitializePopulation_h_
#define _InitializePopulation_h_

#include "random.h"
#include <fstream>
#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> InitializePopulation(unsigned int, unsigned int, int);
int intervalFinder(double, unsigned int);

#endif
